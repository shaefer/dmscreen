DROP TABLE if exists GM_PLOTS;
CREATE TABLE 
if not exists 
GM_PLOTS (
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	PLOT VARCHAR_IGNORECASE
);


DROP TABLE if exists GM_BACKGROUNDS;
CREATE TABLE 
if not exists 
GM_BACKGROUNDS (
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	BACKGROUND VARCHAR_IGNORECASE
);

DROP TABLE if exists gm_quest_items;
CREATE TABLE 
if not exists 
gm_quest_items (
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	QUEST_ITEM VARCHAR_IGNORECASE
);

DROP TABLE if exists gm_npc_characteristics_physical;
CREATE TABLE 
if not exists 
gm_npc_characteristics_physical (
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	CHARACTERISTIC VARCHAR_IGNORECASE
);

DROP TABLE if exists gm_npc_characteristics;
CREATE TABLE 
if not exists 
gm_npc_characteristics (
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	CHARACTERISTIC VARCHAR_IGNORECASE
);

DROP TABLE if exists gm_npc_goals;
CREATE TABLE 
if not exists 
gm_npc_goals (
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	goal VARCHAR_IGNORECASE
);

DROP TABLE if exists gm_plot_twists;
CREATE TABLE 
if not exists 
gm_plot_twists (
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	plot_twist VARCHAR_IGNORECASE
);

DROP TABLE if exists gm_rewards;
CREATE TABLE 
if not exists 
gm_rewards (
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	REWARD VARCHAR_IGNORECASE
);

DROP TABLE if exists gm_secrets;
CREATE TABLE 
if not exists 
gm_secrets (
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	SECRET VARCHAR_IGNORECASE
);

DROP TABLE if exists gm_dungeon_locations;
CREATE TABLE 
if not exists 
gm_dungeon_locations (
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	LOCATION VARCHAR_IGNORECASE
);
DROP TABLE if exists gm_dungeon_types;
CREATE TABLE 
if not exists 
gm_dungeon_types (
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	DUNGEON_TYPE VARCHAR_IGNORECASE
);
DROP TABLE if exists gm_dungeon_entrances;
CREATE TABLE 
if not exists 
gm_dungeon_entrances (
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	ENTRANCE VARCHAR_IGNORECASE
);
DROP TABLE if exists gm_dungeon_rooms;
CREATE TABLE 
if not exists 
gm_dungeon_rooms (
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	ROOM VARCHAR_IGNORECASE
);
DROP TABLE if exists gm_mundane_room_characteristics;
CREATE TABLE 
if not exists 
gm_mundane_room_characteristics (
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	CHARACTERISTIC VARCHAR_IGNORECASE
);
DROP TABLE if exists gm_exotic_room_characteristics;
CREATE TABLE 
if not exists 
gm_exotic_room_characteristics (
	ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) PRIMARY KEY,
	CHARACTERISTIC VARCHAR_IGNORECASE
);