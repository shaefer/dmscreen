{
  "name": "cleanJsonScript",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "aws-sdk": "^2.413.0",
    "fs": "0.0.1-security"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "aws": "npx babel-node CleanJson.js"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "npx": "^10.2.0"
  }
}
